#include "physics_empty.h"

namespace hg
{
    PhysicsEmpty::PhysicsEmpty(PhysicsScene& scene, int id, Level& level, int entity_id)
        : PhysicsComponent(scene, id, level, entity_id)
    {

    }
}